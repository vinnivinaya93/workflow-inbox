services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_PASSWORD: inbox
      POSTGRES_USER: inbox
      POSTGRES_DB: inbox
    ports: ["5432:5432"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U inbox"]
      interval: 2s
      timeout: 3s
      retries: 20
